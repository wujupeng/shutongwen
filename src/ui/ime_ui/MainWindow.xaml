<Window
    x:Class="ShuTongWen.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShuTongWen.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ShuTongWen IME"
    Height="300"
    Width="400"
    IsTitleBarVisible="False"
    Background="Transparent"
    ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="CompositionPanel" Grid.Row="0" Orientation="Horizontal" 
                    Background="{ThemeResource SystemControlAcrylicElementMediumHighBrush}"
                    Padding="12, 8">
            <TextBlock x:Name="CompositionText" FontSize="18" 
                       FontFamily="Microsoft YaHei UI"
                       Foreground="{ThemeResource SystemForegroundColor}"/>
            <Rectangle x:Name="Cursor" Width="2" Height="24" Fill="{ThemeResource SystemAccentColor}"
                       Margin="2, 0"/>
        </StackPanel>

        <ListView x:Name="CandidateList" Grid.Row="1"
                  Background="{ThemeResource SystemControlAcrylicElementMediumHighBrush}"
                  SelectionChanged="CandidateList_SelectionChanged"
                  ItemTemplate="{StaticResource CandidateItemTemplate}"
                  Margin="0, 4, 0, 0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="12, 8"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{ThemeResource SystemAccentColor}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Window>